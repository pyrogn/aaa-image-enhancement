services:
  main_app:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - detector
      - enhancer
    networks:
      - app-network

  detector:
    build:
      context: .
      dockerfile: ./models/detector/Dockerfile
    ports:
      - "8001:8000"
    networks:
      - app-network

  enhancer:
    build:
      context: .
      dockerfile: ./models/enhancer/Dockerfile
    ports:
      - "8002:8000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
